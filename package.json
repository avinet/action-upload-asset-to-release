{
  "name": "action-upload-asset-to-release",
  "version": "0.0.1",
  "description": "Upload asset to a GitHub release.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && ncc build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Asplan Viak AS",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.0.0",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.7.4"
  }
}
